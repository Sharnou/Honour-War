name: Godot project validation

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  GODOT_VERSION: "4.7.2"
  GODOT_TAG: "4.7.2-stable"
  GODOT_BINARY: "Godot_v4.7.2-stable_linux.x86_64"

# Validate only the newest main revision. GitHub Actions otherwise permits
# multiple workflow runs to execute concurrently; canceling stale validation
# saves runner time while keeping the newest commit as the authoritative gate.
concurrency:
  group: honour-war-godot-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout validation commit
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          ref: ${{ github.sha }}

      - name: Report validation source
        shell: bash
        run: |
          set -euo pipefail
          actual="$(git rev-parse HEAD)"
          expected="${GITHUB_SHA}"
          echo "Validation HEAD: ${actual}"
          echo "Workflow SHA:    ${expected}"
          test "${actual}" = "${expected}"

      - name: Run Game3D integrity regression QA
        shell: bash
        run: python3 tools/game3d_integrity_qa.py

      - name: Run Visual RAG-first production gap QA
        shell: bash
        run: python3 tools/visual_gap_register_qa.py

      - name: Download and verify Godot 4.7.2 stable
        shell: bash
        run: |
          set -euo pipefail
          rm -rf godot.zip godot-dist
          mkdir -p godot-dist
          urls=(
            "https://github.com/godotengine/godot-builds/releases/download/${GODOT_TAG}/${GODOT_BINARY}.zip"
            "https://github.com/godotengine/godot/releases/download/${GODOT_TAG}/${GODOT_BINARY}.zip"
          )
          downloaded=0
          for url in "${urls[@]}"; do
            echo "Trying ${url}"
            rm -f godot.zip
            if curl --location --fail --silent --show-error --retry 8 --retry-delay 3 --retry-all-errors --connect-timeout 30 --max-time 600 --output godot.zip "${url}"; then
              if unzip -tq godot.zip >/dev/null 2>&1; then
                downloaded=1
                echo "Downloaded a valid Godot archive from ${url}"
                break
              fi
            fi
          done
          if [ "${downloaded}" -ne 1 ]; then
            echo "ERROR: Could not download a valid Godot ${GODOT_VERSION} archive."
            exit 1
          fi
          unzip -q godot.zip -d godot-dist
          godot_path="$(find godot-dist -type f -name "${GODOT_BINARY}" -print -quit)"
          test -n "${godot_path}"
          chmod +x "${godot_path}"
          version_output="$("${godot_path}" --version 2>&1)"
          echo "Godot version: ${version_output}"
          printf '%s\n' "${version_output}" | grep -E '^4\.7\.2\.stable' >/dev/null
          cp "${godot_path}" "./${GODOT_BINARY}"
          chmod +x "./${GODOT_BINARY}"

      - name: Run SS rental regression QA
        shell: bash
        run: python3 tools/ss_rental_qa.py

      - name: Validate scripts, scenes, and Godot 4.7 target
        shell: bash
        run: |
          set -euo pipefail
          grep -F 'config/features=PackedStringArray("4.7")' project.godot >/dev/null
          ./"${GODOT_BINARY}" --headless --editor --path . --rendering-method gl_compatibility --rendering-driver opengl3 --quit 2>&1 | tee godot-validation.log
          if grep -E 'SCRIPT ERROR|Parse Error|ERROR: Failed to load script|ERROR: Cannot load scene' godot-validation.log; then
            echo 'Godot validation reported errors.'
            exit 1
          fi

      - name: Run hero/monster movement and skill QA
        shell: bash
        run: |
          set -euo pipefail
          ./"${GODOT_BINARY}" --headless --path . --script res://tests/hero_monster_movement_skill_qa.gd 2>&1 | tee hero-monster-movement-skill-qa.log
          grep -F 'HERO_MONSTER_MOVEMENT_SKILL_QA: PASS' hero-monster-movement-skill-qa.log >/dev/null
          if grep -F 'HERO_MONSTER_MOVEMENT_SKILL_QA: FAIL' hero-monster-movement-skill-qa.log; then
            echo 'Hero/monster movement and skill QA reported FAIL.'
            exit 1
          fi

      - name: Run authoritative monster movement/facing QA
        shell: bash
        run: |
          set -euo pipefail
          ./"${GODOT_BINARY}" --headless --path . --script res://tests/monster_motion_authority_qa.gd 2>&1 | tee monster-motion-authority-qa.log
          grep -F 'MONSTER_MOTION_AUTHORITY_QA: PASS' monster-motion-authority-qa.log >/dev/null

      - name: Capture real running-game screenshot
        shell: bash
        run: |
          set -euo pipefail
          rm -rf artifacts
          mkdir -p artifacts
          ./"${GODOT_BINARY}" --headless --path . --rendering-method gl_compatibility --rendering-driver opengl3 --script res://tests/capture_real_game_screenshot.gd 2>&1 | tee real-game-screenshot.log
          grep -F 'REAL_GAME_SCREENSHOT_PASS:' real-game-screenshot.log >/dev/null
          test -s artifacts/honour-war-real-game.png
          file artifacts/honour-war-real-game.png

      - name: Upload Godot validation log and real gameplay evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: godot-project-validation-and-real-game-evidence
          path: |
            godot-validation.log
            hero-monster-movement-skill-qa.log
            monster-motion-authority-qa.log
            real-game-screenshot.log
            artifacts/honour-war-real-game.png
          if-no-files-found: ignore
