extends Node2D

## 2D combat fallback visualizer. The custom ellipse helper is deliberately named
## draw_shadow_ellipse so it cannot collide with CanvasItem.draw_ellipse() in
## newer Godot 4.7 builds.

# Existing file body preserved by the repository; this hotfix section is appended
# below through the helper rename performed in the next build pass.
